export {
  extractNameOfExchangeFromURL,
  checkLackOfEmailInPassword,
  getCreditCardType,
  getDefaultCurrency,
  getDefaultLang,
  getDocumentScrollbarWidth,
  getScrollbarWidth,
  getQueryString,
  sortByString,
  sortByDate,
  sortByNumber,
  getRandomColor,
  getPlural,
} from '@utils/helpers';

export { historyApp } from '@utils/history';

export {
  formatDate,
  formatToPrettyIntegerNumber,
  intlDateTime,
  intlTime,
  intlMonth,
  intlDay,
  getDataTime,
  percentRound2,
  round,
  normalizeCardExpire,
  normalizeCardNumber,
  normalizeCVV,
} from '@utils/format';

export { LS } from '@utils/LS';

export { notify } from '@utils/notify';

export { appUI } from '@utils/ui';

export { patterns } from '@utils/patterns';
