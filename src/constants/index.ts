/**
 * @summary main entry to constants
 */
export { CODE_ERROR_UNAUTHORIZED } from '@constants/error';

export { API_URL, API_VERSION } from '@constants/env';

export { coinColors, DEFAULT_TICKERS_PAIR, MAIN_COINS, COINS } from '@constants/coins';

export { DEFAULT_LANG, DEFAULT_CURRENCY, localStorageKeyLang } from '@constants/lang';

export { UserMode, PlanType } from '@constants/user';

export { Paths } from '@constants/paths';
